package dev.samfaina.percentagebarview.utils

/**
 * Constants to define default animation values
 */
class AnimationConstants {
    companion object {
        const val DEFAULT_DELAY: Long = 600
        const val DEFAULT_DURATION: Int = 800
    }
}

/**
 * Constants to define threshold text position
 */
class PositionConstants {

    companion object {
        const val TOP = 0
        const val BOTTOM = 1
    }
}
